// rust-web3-10weeks · Tuần 01 · Bài 04: Tổng quan về Kiến trúc Solana & SVM (Rust Theory).
fn main() {
    let values = [4, 5, 6];
    let total: i32 = values.iter().sum();
    println!("04 - Tổng quan về Kiến trúc Solana & SVM (Rust Theory): {total}");
}
