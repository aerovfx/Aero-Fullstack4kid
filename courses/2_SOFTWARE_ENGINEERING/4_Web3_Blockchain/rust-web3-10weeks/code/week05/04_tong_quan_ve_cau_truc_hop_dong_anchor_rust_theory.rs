// rust-web3-10weeks · Tuần 05 · Bài 04: Tổng quan về Cấu trúc Hợp đồng Anchor (Rust Theory).
fn main() {
    let values = [4, 5, 6];
    let total: i32 = values.iter().sum();
    println!("04 - Tổng quan về Cấu trúc Hợp đồng Anchor (Rust Theory): {total}");
}
