// rust-web3-10weeks · Tuần 02 · Bài 04: Tổng quan về Mô hình Tài khoản Solana (Rust Theory).
fn main() {
    let values = [4, 5, 6];
    let total: i32 = values.iter().sum();
    println!("04 - Tổng quan về Mô hình Tài khoản Solana (Rust Theory): {total}");
}
