// rust-fundamentals-10weeks · Tuần 10 · Bài 03: Capstone — chọn một.
fn main() {
    let values = [3, 4, 5];
    let total: i32 = values.iter().sum();
    println!("03 - Capstone — chọn một: {total}");
}
