// rust-backend-10weeks · Tuần 07 · Bài 04: Tổng quan về Định tuyến Router & Xử lý JSON (Rust Theory).
fn main() {
    let values = [4, 5, 6];
    let total: i32 = values.iter().sum();
    println!("04 - Tổng quan về Định tuyến Router & Xử lý JSON (Rust Theory): {total}");
}
