// rust-backend-10weeks · Tuần 05 · Bài 04: Tổng quan về Tokio Runtime & Lập trình Bất đồng bộ (Rust Theory).
fn main() {
    let values = [4, 5, 6];
    let total: i32 = values.iter().sum();
    println!("04 - Tổng quan về Tokio Runtime & Lập trình Bất đồng bộ (Rust Theory): {total}");
}
